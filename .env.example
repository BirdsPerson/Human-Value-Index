# Required. Set in the Netlify dashboard for deploys; locally, `netlify dev` reads this file.
ANTHROPIC_API_KEY=

# Optional. Model used for evaluations. Defaults to claude-opus-5.
# claude-sonnet-5 is cheaper and faster if evaluations approach the function timeout.
# HVI_MODEL=claude-opus-5
